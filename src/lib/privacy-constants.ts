/**
 * Constantes de privacidade compartilhadas entre servidor e cliente.
 *
 * Este arquivo não importa banco nem APIs do Next — pode ser usado em
 * componentes de cliente (`"use client"`) sem arrastar código de servidor.
 */

/** Texto exato que o usuário precisa digitar para confirmar a exclusão. */
export const DELETE_CONFIRMATION_WORD = "EXCLUIR";
